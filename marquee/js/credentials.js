/* Marquee Event System — operator credentials.
 *
 * No username and no password live in this file. What it holds is a random salt and a
 * short token that only the right credentials can decrypt. Replace the null below with
 * the block set-password.html gives you, then commit it.
 *
 *   1. Open set-password.html on your published site (or over http://localhost — the
 *      browser's crypto API will not run from a file:// page).
 *   2. Choose the operator name and password.
 *   3. Paste what it gives you over this whole file and push.
 *
 * Until then the console will say the door has no lock fitted and refuse to open.
 */

export const CREDENTIALS = null;
